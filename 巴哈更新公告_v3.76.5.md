【更新】v3.76.5|序號開通、游標被搶回修好、自動更新上線、新地圖與新裝備補翻

重點先列:

* **新增「序號」功能** —— 沒有加入指定公會的人,拿到序號一樣能開啟全部功能
* **修好滑鼠游標**:過圖 / 人物死亡 / 切視窗之後會變回 Windows 預設 —— 這個困擾很久了
* **自動更新正式上線**:設定工具會自己檢查新版,而且**會告訴你這版改了什麼**
* **新內容補翻**:暮色系列武器防具、翼耳、黑暗莊園 / 黑暗要塞的回聲怪
* **譯文太長撐破框會自動縮字** —— 地圖的怪物清單不再互相重疊

---

## 一、序號:不用加入公會也能用

以前功能要開啟,只能靠「你的公會在允許名單裡」。現在多了一條路:**跟作者拿一組序號**。

設定工具多了一個 **「序號」分頁**(排在第 3 個,一開就看得到),照上面的 ①②③ 三步走:

1. **把你的帳號識別給我** —— 按「複製申請訊息」,會複製一段寫好的文字,直接貼給我就行
2. **貼上序號** → 按「套用序號」
3. **重開遊戲**

有些序號會多一顆「**啟用**」按鈕,按下去就綁定你的 Steam 帳號 —— 之後**別人拿到你的序號也用不了**,但你自己重灌、換電腦都可以再啟用一次,不用重新跟我要。

成功或失敗都會**跳視窗告訴你**,不會只是默默不動。

> 想確認有沒有生效:進遊戲一次,再到「關於」頁按「產生診斷報告」,最上面那行會寫「序號: …」。

**公會允許名單完全沒有變動** —— 原本在名單裡的公會照舊,不受任何影響。

## 二、滑鼠游標被搶回去 —— 修好了

網友回報:**過圖、人物死亡、切出去再切回來**之後,自訂游標就變回 Windows 內建的箭頭。

原因是這三種情況都是**遊戲自己把游標設回去**,而外掛只攔得到「ESC 開關選單」那一種。追不完的路徑就別追了 —— 改成**每秒自動補回一次**,不管是什麼原因被搶走,最多一秒就會恢復。

舊設定不用改,更新後自動生效。

## 三、自動更新上線(而且會說改了什麼)

設定工具啟動時會自己去看有沒有新版:

* 有新版 → 跳出視窗,**列出這版改了什麼**,問你要不要下載
* 下載完 → 驗證檔案完整性,再問你要不要安裝(**安裝會保留你目前的所有設定**)
* 「關於」頁可以設成:只通知 / 自動下載 / 完全不檢查

安全性的部分:更新資訊有 **RSA 數位簽章**,設定工具驗過簽章才會提示;下載完還會比對 **SHA256**。就算我的 GitHub 帳號被盜,對方沒有我的私鑰,推不出你的工具會接受的更新。更新說明也在簽章保護範圍內,不可能被拿來塞廣告或釣魚連結。

> 這版**還是要手動更新一次**(舊版工具還沒有這個功能),之後就會自動提示了。

## 四、新內容補翻

**黑暗莊園(Lv145)/ 黑暗要塞(Lv150–155)**

* 回聲怪 16 隻 + 對應卡片:迴音信徒 / 法師 / 盜賊 / 斥候 / 召喚師 / 戰士 / 騎士 / 織法者,以及 Lv155 的各種「大師」
* 順帶修好一個撞名:`Echo Mage` 與 `Echo Wizard` 以前**兩隻都叫迴音法師**,現在分成迴音法師 / 迴音巫師

**暮色系列(Lv130)**

* 武器 7 件:暮色法典 / 泥沼 / 使者 / 破斧 / 鋒刃 / 災厄 / 收割
* 防具 6 件:暮色鎧甲 / 長袍 / 脛甲 / 護腿 / 靴 / 鞋

**其他**:翼耳 8 種(含「完美的…」)、柴郡貓、陰影騎士靴、腐化核心、靈魂碎片、鋼鐵意志、凍傷等

## 五、排版:譯文撐破框會自動縮字

中文譯文加上雙語通常比英文長,固定高度的清單列就會折行、蓋住下一列 —— 黑暗要塞的怪物清單整批疊在一起就是這樣來的。

現在偵測到放不下時會**自動縮小字級**到剛好塞得下。不喜歡的話可以關掉(`SpiritZh_view.txt` 設 `autofit=0`)。

判斷條件有特別限制過,只處理「**框裝得下一行、但文字被迫換行**」的清單列 —— 像地圖區塊標籤、裝備浮動標籤那種本來就設計成會超出框的文字**不會被動到**。

## 六、其他修正

* 「攻速」以前只有它一個帶英文(ASPD),現在跟其他屬性一致
* 修正一個**可能把玩家名稱當成物品名翻掉**的問題(例如有人取名叫某個道具名)
* 玩家 ID 帶數字時不會再被拆開翻譯(例如 `Life99` 以前會變成「生命99」)
* 先驅者、裂界斧、時序法典、利維坦等物品補上雙語顯示
* 漏翻回報用的殘留紀錄大幅減少誤判,之後回報漏翻會更準

---

## 檔案校驗

| 檔案 | 大小 | SHA256 |
|---|---|---|
| 一鍵安裝包 v3.76.5 | 48.94 MB | `05631411A8E86C8A306CCCDA772600039D3F1D7B7B0E7EA93792FECE772FECC9` |
| 公會專用版 v3.76.5 | 55.91 MB | `179937FFD42D0D36A84718D9C4D366563DE5BC8E37F02A0484E2FE96F8AC019F` |

可用 PowerShell 的 `Get-FileHash 檔名 -Algorithm SHA256` 自行核對。

---

## 安裝

1. **關閉遊戲**
2. 解壓縮(不要解在遊戲資料夾裡)
3. 執行 **`安裝.bat`**,照畫面選顯示模式與字型
4. 用 Steam 正常啟動遊戲

已經在用舊版的人直接覆蓋安裝就好,**設定會全部保留**。

回報問題請附「一鍵診斷.bat」產生的桌面報告。
